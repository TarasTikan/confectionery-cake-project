import { RegisterForm } from "../../components/RegisterForm/RegisterForm"

export const RegisterPage = () => {
    
    return (
        <main>
            <section>
                <h1>Register Page</h1>
                <RegisterForm/>
            </section>
        </main>
    )
}